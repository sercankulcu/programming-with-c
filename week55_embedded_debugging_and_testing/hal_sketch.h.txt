/* hal.h — the boundary */
typedef struct {
    bool     (*uart_read)(uint8_t *out);
    void     (*uart_write)(uint8_t byte);
    uint32_t (*ticks)(void);
    void     (*led_set)(bool on);
} Hal;

/* logic.h — pure logic, no registers anywhere */
void protocol_init(Protocol *p, const Hal *hal);
void protocol_poll(Protocol *p);
